// M0 stub: the quiet page subscribes to nothing yet (subscription hub lands with Tier 1).
export function bootstrapQuietPage(): void {
  // Surfaces are authority-free (A-05): this file may not touch storage or browser APIs.
  // Its only job today is to exist so the build and navigation work end-to-end.
}
